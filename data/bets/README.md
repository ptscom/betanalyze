# elections_2 dataset

This branch (`dataset/elections_2`) is for the **elections_2** batch only.

App code matches `cursor/reversal-occurrence-analysis-d286` (reversal occurrence,
period performance, first crossover, rise in MA, etc.).

Add Excel exports here — one file per bet/market. Do not copy files from other
branches unless they belong in this batch.

Then run:

```bash
npm run build:data
```

See `data/DATASETS.md` for the full multi-dataset workflow.
